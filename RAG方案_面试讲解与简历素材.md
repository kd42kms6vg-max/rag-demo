# RAG 企业知识库问答系统 · 技术方案与面试/简历素材

> **如何使用本文档**
> - **给自己**：第四、五、八节是面试逐行讲解 + 高频追问，照着背能讲清整个项目。
> - **给另一个 AI 写简历**：直接把本文档（从"## 给简历 AI 的总述"到"## 九、部署与运维"）发给简历生成 AI，并附一句"请根据以下项目技术方案，帮我写一段实习简历的『项目经历』章节，用 3-5 条 bullet points，突出技术深度与个人贡献"。

---

## 给简历 AI 的总述（可直接复制粘贴给另一个 AI）

项目名：基于 RAG 的企业内部知识库智能问答系统（个人独立开发，已部署上线）。

技术栈：Node.js + Vercel Serverless Functions（云函数）作为后端，阿里云百炼 DashScope 的 text-embedding-v3 做文本向量化、qwen-plus 大模型做生成，前端用原生 HTML + SSE（EventSource）实现流式打字机交互，marked + DOMPurify 渲染 Markdown，部署于 Vercel 平台。

核心能力：用户输入问题后，系统先做"混合检索"——同时用向量余弦相似度和 BM25 关键词算法召回相关文档片段，按 0.6/0.4 加权融合打分（min-max 归一化），取 Top6 再用大模型做重排序取 Top3；随后注入"意图门槛"（Top1 分数低于阈值直接拒答、零 token 成本），最后把检索到的参考资料连同问题一起交给大模型流式生成答案。支持多轮对话（滑动窗口保留最近 6 轮），并按 session 隔离不同用户的对话历史（修复了多设备串台问题）。

个人贡献：独立完成需求分析、架构设计、全部后端检索/重排序/SSE 逻辑、前端流式交互与对话隔离，部署上线并修复了 SSE 状态流混入答案、多用户对话泄漏两个真实缺陷。

---

## 一、项目定位（一句话）

一个**已部署上线**的 RAG（检索增强生成）企业知识库问答应用：把公司制度文档变成"可对话的 AI 助手"，支持流式回答、多轮对话、多用户隔离。

## 二、技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| 后端运行时 | Node.js（Vercel Serverless Functions） | `api/rag.js` 即云函数入口 |
| 向量化 | 阿里云百炼 `text-embedding-v3` | 文档与问题转向量 |
| 生成模型 | 阿里云百炼 `qwen-plus` | 流式 SSE 生成 |
| 检索算法 | 向量余弦 + BM25（混合） | 0.6 / 0.4 融合 |
| 前端 | 原生 HTML + EventSource(SSE) + marked + DOMPurify | 打字机流式渲染 |
| 部署 | Vercel | 静态 + 云函数，环境变量托管密钥 |

## 三、系统架构与数据流

```
用户提问（前端）
   │  EventSource /ai/rag/stream?question=...&sid=...
   ▼
api/rag.js (Vercel 函数)
   1. 会话隔离：按 sid 取本会话历史（Map<sid, history>）
   2. 知识库懒加载（首次请求）：
      test.md → 分块(chunkText) → 逐块向量化(getEmbedding) → 建 BM25 索引(buildBM25)
   3. 检索(retrieve)：
      问题向量化 → 余弦相似度 + BM25 打分 → min-max 归一化 → 0.6/0.4 融合
      → 排序取 Top6 → LLM 重排序(llmRerank) → Top3
   4. 意图门槛：Top1 分数 < 0.12 → 直接返回安全话术（零 token 成本）
   5. 生成：buildMessages 拼 Prompt → qwen-plus 流式 → SSE 回传
   6. 多轮：滑动窗口保留最近 6 轮写入会话历史
   │  SSE 事件流（type:'status' / type:'content'）
   ▼
前端：status 单独渲染状态栏；content 累加成 Markdown 打字机
```

## 四、核心代码逐函数讲解（api/rag.js，共 13 个函数）

### 1. `httpsPost`（行 25-40）—— 带超时的 HTTPS 封装
- **职责**：向 DashScope 发 JSON POST，返回解析后的对象；30s 超时。
- **关键点**：手动用内置 `https` 模块（零额外依赖）；`setTimeout(30000)` + `req.destroy()` 防挂死。
- **面试会怎么问**："为什么不用 axios/fetch？"答：Serverless 环境追求零依赖、冷启动快，内置 `https` 够用且可控。
- **面试会怎么问**："超时怎么处理？"答：主动 `req.destroy()` 并 reject，避免函数被一个慢请求拖垮（Vercel 有执行时长上限）。

### 2. `httpsPostStream`（行 43-64）—— SSE 流式解析
- **职责**：解析大模型流式响应（按 `\n` 切分 `data:` 行），逐块回调 `onChunk`。
- **关键点**：用 `buf` 缓冲跨块不完整行；只处理以 `data:` 开头的行。
- **面试会怎么问**："SSE 和普通 JSON 接口区别？"答：SSE 是服务端单向推送，一条连接上分多次返回，首字延迟低、用户体验好，适合生成式回答。

### 3. `getEmbedding`（行 67-70）—— 文本向量化
- **职责**：调 `text-embedding-v3` 把文本转成向量数组。
- **面试会怎么问**："Embedding 是什么？"答：把文本语义映射成高维向量，语义相近的文本向量距离近，是向量检索的基础。

### 4. `cosineSimilarity`（行 73-77）—— 余弦相似度
- **职责**：计算两个向量的余弦相似度（夹角余弦），返回 -1~1。
- **关键点**：`dot / (|a|*|b|)`，纯数学实现。
- **面试会怎么问（高频！）**："余弦相似度和欧氏距离区别？"答：余弦只看方向不看模长，更适合文本（长短不影响语义相似度判断）；欧氏距离受文本长度影响。
- **⚠️ 你的训练重点**：这一小段（约 5 行）要求**脱离 AI 纯手写**——面试常让你白板写 `cosineSimilarity`，必须能默写。

### 5. `tokenize`（行 80-84）—— 中英文分词
- **职责**：正则 `/[a-zA-Z0-9]+|[\u4e00-\u9fff]/g` 把文本切成词元，英文小写、中文逐字。
- **关键点**：中文没有空格，按字切（简单但够用）；英文按词。

### 6. `buildBM25`（行 85-93）—— 建 BM25 索引
- **职责**：遍历文档算词频 `tf`、文档频率 `df`、平均文档长度 `avgdl`。
- **面试会怎么问**："BM25 比 TF-IDF 好在哪？"答：BM25 引入文档长度归一化和饱和函数，长文档不会因词频虚高，更鲁棒。

### 7. `bm25Score`（行 94-102）—— BM25 打分
- **职责**：标准 BM25 公式，`k1=1.5, b=0.75`。
- **关键点**：`idf * (f*(k1+1)) / (f + k1*(1-b+b*dl/avgdl))`。

### 8. `chunkText`（行 105-116）—— 文档分块
- **职责**：按段落切分，标题行独立成块，块大小 300 字、重叠 50 字。
- **关键点**：重叠（overlap）避免边界处语义被切断。
- **面试会怎么问**："为什么要分块、为什么重叠？"答：大模型有上下文上限；重叠防止一个完整语义被块边界劈开导致检索不到。

### 9. `initKB`（行 119-132）—— 知识库懒加载 + 防重入
- **职责**：首次请求时把文档分块、向量化、建 BM25 索引。
- **关键点**：`kbReady` 标志 + `kbInitializing` 防重入（并发请求时不重复初始化，轮询等待）。
- **面试会怎么问**："为什么懒加载而不是启动时？"答：Serverless 函数冷启动，启动时加载会拖慢首次响应；懒加载把成本摊到第一次请求，且 `kbReady` 后热实例复用不再重复。
- **面试会怎么问**："并发请求同时触发初始化怎么办？"答：用 `kbInitializing` 标志 + 轮询等待（行 121），避免重复向量化浪费配额。

### 10. `llmRerank`（行 135-152）—— LLM 重排序
- **职责**：把 Top6 候选编号后交给大模型，让它挑出最相关的 3 段。
- **关键点**：容错——解析失败 `catch` 回落到 `candidates.slice(0,3)`；正则提取 `[3,1,2]` 数组。
- **面试会怎么问**："为什么向量检索后还要 LLM 重排？"答：向量检索是"粗排"，语义近似不等于答案相关；LLM 重排用更强语义理解做"精排"，提升精度。这是 RAG 工程常见优化。

### 11. `retrieve`（行 155-165）—— 混合检索（项目灵魂）
- **职责**：问题向量化 → 算每篇余弦 + BM25 → **min-max 归一化** → `0.6*向量 + 0.4*BM25` 融合 → 排序取 Top6 → LLM 重排取 Top3。
- **关键点**：向量和 BM25 量纲不同，必须归一化才能加权；权重 0.6/0.4 是经验值。
- **面试会怎么问（核心！）**："为什么用混合检索而不是纯向量？"答：纯向量擅长语义召回但漏掉关键词精确匹配（如专有名词、编号）；BM25 补关键词召回；融合兼顾语义与字面，召回更稳。
- **面试会怎么问**："归一化为什么用 min-max？"答：余弦和 BM25 分数范围不同，直接加权会被量纲大的主导，min-max 拉到 0~1 同尺度再融合。

### 12. `buildMessages`（行 168-175）—— Prompt 构造 + 安全护栏
- **职责**：拼 system（职责限定 + 拒答话术）+ user（历史 + 参考资料 + 问题）。
- **关键点**：**意图门槛**写在 system 里——无关问题只回固定话术；不透露 AI 身份与技术细节；多轮历史只取最近 6 轮。
- **面试会怎么问**："怎么防止大模型乱回答/越权？"答：Prompt 层做职责边界 + 固定话术；配合检索层阈值拒答（Top1<0.12 直接不调生成），双重防护且省成本。

### 13. 主 Handler（行 178-236）—— 流程编排
- **职责**：入口。取 question/sid → 空值校验 → 取本会话历史 → SSE 头 → kb 懒加载 → retrieve → 意图门槛 → 流式生成 → 写回历史。
- **关键点**：`type:'status'`（初始化提示）与 `type:'content'`（正式答案）**分通道**，这是修复 UX bug 的关键（详见第五节）；异常 `catch` 返回"服务暂时不可用"。
- **面试会怎么问**："流式输出怎么保证顺序？"答：SSE 单连接顺序到达，前端按接收顺序累加渲染即可。

---

## 五、会话隔离方案（你发现的真实 bug 修复 —— 面试加分项）

- **现象**：两台设备测试，设备 A 问的"年假/你几岁"出现在设备 B 的对话历史里。
- **根因**：原代码 `conversationHistory` 是模块级**全局数组**，Vercel 热实例复用机制让所有用户共享同一份内存。
- **修复**：
  1. `api/_lib/sessions.js`：用 `Map<sid, history>` 按会话隔离，带 `MAX_SESSIONS=2000` 容量保护（超出清理最早一半，防孤儿会话堆积）。
  2. 前端 `sessionStorage` 给每个标签页生成独立 `sid`，随每次请求带 `&sid=`。
  3. "新对话"通过**前端轮换 sid** 实现（旧 sid 被孤立弃用）——因为 Vercel 各 api/*.js 是独立函数、内存不共享，无法跨函数删除。
- **面试话术**："我发现并修复了一个多租户隔离缺陷：Serverless 热实例共享内存导致对话跨用户泄漏，改用 session 级 Map 隔离 + 前端 sessionId 解决。"
- **延伸考点**：Serverless 无状态特性、内存状态必须外置（生产应换 Redis/数据库）。

## 六、前端 SSE 处理（public/index.html）

- **双通道**：`obj.type==='status'` → 仅更新状态栏（如"正在初始化…"），**不混入答案**；`obj.type==='content'` → 累加 Markdown 打字机，首字到达即移除状态栏。
- **为什么这样设计**：最初版本把初始化提示当 `content` 发，导致"正在初始化"混进正式回答（你发现的 UX bug），分通道后解决。
- EventSource 连接：`/ai/rag/stream?question=...&sid=...`，`[DONE]` 结束。

## 七、简历可复制成就点（bullet points）

> 复制下面任意几条到简历"项目经历"里，已按"动作+技术+结果"结构写好：

1. 独立设计并实现基于 RAG 的企业知识库问答系统，后端采用 Vercel Serverless Functions，集成阿里云百炼 text-embedding-v3 与 qwen-plus，已部署上线并支持流式交互。
2. 实现**混合检索**（向量余弦 + BM25，min-max 归一化后 0.6/0.4 融合）与 **LLM 重排序**，相比纯向量检索提升召回准确率与答案相关性。
3. 设计**意图门槛**机制（检索分数低于阈值直接拒答），在不调用大模型的前提下拦截无关问题，实现零 token 成本过滤。
4. 修复多用户对话泄漏缺陷：识别 Serverless 热实例共享内存导致会话串台的根因，引入 session 级隔离 + 前端 sessionId，保障多租户数据隔离。
5. 采用 SSE 流式协议实现打字机式回答，并分离"状态流"与"内容流"通道，解决状态提示混入正式答案的体验问题。

## 八、面试高频追问清单（附应答要点）

| 问题 | 应答要点 |
|---|---|
| 长上下文模型（百万 token）出来，RAG 还有必要吗？ | 有必要且互补：RAG 解决成本（只发相关片段）、隐私（整库不出域）、实时性（增量更新）、"中间遗忘"问题（精准前置）。长上下文+RAG 是 2026 主流架构。 |
| 为什么混合检索而不是纯向量？ | 向量漏精确关键词（编号/专有名词），BM25 补召回，融合更稳。 |
| SSE 和 WebSocket 区别？为何选 SSE？ | SSE 单向服务端推送、基于 HTTP、自动重连、实现简单；问答只需服务端→客户端，无需双向，故选 SSE。 |
| Serverless 冷启动怎么处理？ | 知识库懒加载 + `kbReady` 热实例复用；`kbInitializing` 防并发重入；生产可把向量库外置（如 pgvector）消除冷启动初始化。 |
| 向量数据怎么持久化？现在内存里重启就丢？ | 当前为演示版，向量存内存（热实例复用可扛）；生产应落向量数据库（Chroma/pgvector/Milvus），前端知识库变更时增量重建索引。 |
| 意图门槛 0.12 怎么定的？ | 经验阈值，需按真实问答集调参；也可改用"Top3 分数方差""LLM 判相关"等更鲁棒策略。 |
| 多用户隔离在生产怎么做？ | 当前 session Map 仅热实例内有效，重启即失；生产应外置到 Redis 或数据库，并加 TTL 过期。 |

## 九、部署与运维

- **部署**：`vercel --prod`；`vercel.json` 把 `/ai/rag/stream` 等路由重写到 `api/rag`。
- **密钥管理**：`DASHSCOPE_API_KEY` 走 Vercel 环境变量 + `.env`（gitignore），**不进代码库**（曾因明文泄漏做过轮换清理）。
- **已知限制（诚实交代）**：
  - 向量库在内存，重启/新实例需重新初始化（冷启动延迟）；
  - 会话历史在内存，未持久化；
  - 单文档知识库（test.md），未做多源/增量更新。
  - 这些恰恰是你"下一步可聊的优化点"，面试时主动提显得有工程判断力。

---

*文档生成日期：2026-09-04 · 基于项目真实代码 `api/rag.js` / `api/_lib/sessions.js` / `public/index.html` 编写，可用于面试准备与简历生成。*
