# 企业制度 RAG 问答助手 · 项目复盘总结

> 撰写时间：2026-08-08 ｜ 最后更新：2026-08-08
> 说明：本报告基于**实际代码与 git 历史核查**撰写，不是凭记忆。核查发现项目当前形态与文档/提交记录存在多处不一致，已在对应章节标出。
> 更新记录：补充「多设备对话串台」关键 bug（根因 + 解决方案）、标注 git 提交已完成、补充 RAG vs 长上下文分析文档引用。

---

## 1. 功能与任务回顾（已完成 / 未完成）

### 1.1 已完成（已在线上运行，Vercel 版）

| 模块 | 内容 | 证据 |
|------|------|------|
| **RAG 全链路** | 分块 → 向量化 → 混合检索 → LLM 重排序 → 生成答案 | `api/rag.js` 全文件 |
| **混合检索** | 向量语义检索（余弦相似度）+ BM25 词法检索，线性归一化加权融合（0.6 / 0.4） | `retrieve()` `api/rag.js:153-163` |
| **LLM 重排序** | 候选段落 + 问题交给 `qwen-plus` 精排 Top-3 | `llmRerank()` `api/rag.js:133-150` |
| **流式输出（SSE）** | 打字机效果，逐字返回 | `httpsPostStream()` `api/rag.js:41-62` |
| **多轮对话记忆** | 滑动窗口保留最近 6 轮，支持指代消解 | `buildMessages()` `api/rag.js:168` |
| **会话隔离（你发现的 bug）** | 按 `sessionId` 隔离对话历史，多设备/多用户不串台 | `api/_lib/sessions.js` + 前端 `sessionStorage` |
| **意图门槛（防白嫖）** | 最高分 < 0.12 直接拒答，不调 LLM，零 token 消耗 | `THRESHOLD` `api/rag.js:197-201` |
| **安全话术** | 不暴露 AI 身份、不答无关领域、统一柔和拒答 | system prompt `api/rag.js:168` |
| **前端书卷风 UI** | 暖纸色主题、衬线字体、阅读栏排版、Markdown 渲染 | `public/index.html` |
| **明暗主题切换** | 切换并持久化到 `localStorage` | `index.html:363-374` |
| **"新对话"重置** | 调 `/ai/reset` 清空后端记忆 | `index.html:377-380` |
| **UX 修复（你提的需求）** | "正在初始化知识库"作为 `type:'status'` 单独走状态栏，不混入答案 | 后端 `api/rag.js:194`；前端 `index.html:439-457` |
| **前端安全** | `marked` + `DOMPurify` 渲染，防 XSS | `index.html:351-454` |
| **云端部署** | Vercel Serverless 上线，公网可访问 | `vercel.json` + `api/` |
| **密钥安全** | `.env` 被 gitignore，Key 走平台环境变量 | `.gitignore` 存在；Dashboard 配置 |
| **去重依赖瘦身** | 移除 LangChain，自写分块，71MB → 8.2MB | git log `d335ea5` |
| **求职物料** | README + 面试演示视频脚本 | `README.md` / `演示视频脚本.md` |

### 1.2 未完成 / 半成品

- **向量库持久化**：当前每次冷启动都重新 `initKB()` 全量向量化（`api/rag.js:117-130`）。生产应接 Chroma / pgvector / Milvus。
- **多格式文档 ingestion**：只支持 `data/test.md`（Markdown），不支持 PDF / Word / 网页。
- **鉴权与限流**：公网部署后无防护，Key 有被盗刷风险。
- **语义缓存 / 可观测**：高频问题无缓存；无 token 用量、回答质量监控。
- **自动化测试**：零测试，检索/重排序/拒答逻辑全靠手动验证。
- **Java Spring Boot 版本**：`pom.xml` + `RagDemoApplication.java` 是**空脚手架**（`application.properties` 仅一行 `spring.application.name`），未实现任何 RAG 逻辑——属于探索/放弃方向。
- ~~**git 提交缺失**~~ ✅ 已解决：Vercel 版改动（含会话隔离修复 + 两份分析文档）已提交进版本库（见 git 最新提交）。

### 1.3 ⚠️ 文档与代码已不一致（需立即修）

- **README 严重过时**：仍描述已删除的 `rag-advanced.js`、Express、LangChain、Render/FC 部署；实际跑的是 `api/rag.js`（无 Express、无 LangChain、Vercel）。
- **演示视频脚本过时**：脚本里写 `node rag-advanced.js` + `localhost:8080`，与 Vercel 部署不符。
- 这两份文档如果拿去面试，会被一眼看穿"代码和文档对不上"，反而减分。

---

## 2. 关键错误与问题（根本原因 + 解决方案）

| # | 问题 | 根本原因 | 解决方案 | 现状 |
|---|------|----------|----------|------|
| 1 | **FC 强制下载 HTML**（`Content-Disposition: attachment`） | 阿里云 FC3 HTTP 触发器代理层**无条件注入** `attachment`，无视用户设的 `inline`；curl 正常、浏览器必弹下载 | 放弃 FC，迁移到 Vercel（原样转发响应头） | ✅ 已解决 |
| 2 | Vercel 报错 `framework "node" must specify entrypoint` | CLI 把根目录遗留的 `server.js` 误判为 "services" 框架 | 归档旧 FC/Node 文件到 `_archive/` | ✅ 已解决 |
| 3 | Vercel 报 `Project framework is set to "services"` | 上一项目的残留配置卡在 Dashboard | `vercel remove` 彻底重建项目 | ✅ 已解决 |
| 4 | **UX bug：初始化提示混入答案** | 后端把状态提示也用 `type:'content'` 发出，前端当答案拼接 | 后端改 `type:'status'`；前端状态栏单独渲染、首内容到达即移除 | ✅ 已解决（你发现的需求） |
| 5 | 依赖膨胀 71MB | 早期用 LangChain.js 全家桶 | 移除 LangChain，自写分块/BM25 | ✅ 已解决（git `d335ea5`） |
| 6 | FC 旧 Node 不兼容可选链 `?.` | 运行时代旧，语法不支持 | 去掉可选链 | ✅ 已解决 |
| 7 | **冷启动慢 / 每次重算向量** | Serverless 无状态，全局 `vectorStore` 冷启动清空 | 半解决：`kbInitializing` 锁防并发重入，但**未做持久化** | ⚠️ 部分解决 |
| 8 | **git 历史与线上代码脱节** | Vercel 版改动从未 commit | 手动提交（含会话隔离修复 + 分析文档） | ✅ 已解决 |
| 9 | **多设备对话串台（你发现的 bug）** | `conversationHistory` 是模块级全局数组，Vercel 热实例复用让所有用户共享同一份内存——设备 A 的历史泄漏到设备 B | 抽 `api/_lib/sessions.js` 用 `Map<sid,历史>` 按会话隔离；前端 `sessionStorage` 生成唯一 sid；"新对话"改为前端轮换 sid（Vercel 多函数内存不共享，跨函数删历史无效） | ✅ 已解决 |

**核心教训**：错误 1 是最值钱的——它证明了"平台机制性限制"不是改代码能解决的，果断换平台比死磕更省时间。错误 4 说明"状态流"和"内容流"在流式协议里必须分通道，这是 SSE 设计的基本功。错误 9 提醒：**Serverless 的函数实例内存是"伪全局"**——模块级变量在热实例内共享、跨实例不共享，任何"全局状态"在无状态架构里都要重新设计隔离与持久化。

---

## 3. 核心技术知识点与实践经验

### 3.1 知识点（可迁移，与技术栈无关）

- **RAG 为什么存在**：用检索约束生成，治大模型"幻觉"；私有知识库问答的标准范式。
- **Embedding + 向量检索**：文本 → 高维向量，余弦相似度衡量语义相近。
- **分块策略**：`CHUNK_SIZE / OVERLAP`（本项目 300/50），按标题切分保留结构。
- **混合检索**：稠密向量（懂同义）+ 稀疏 BM25（精确词），归一化后加权融合——比单路召回更稳。
- **LLM 重排序（Rerank）**：用大模型对候选精排，RAG 工程标配的准确率提升手段。
- **SSE vs WebSocket**：SSE 单向、基于 HTTP、自动重连，适合"服务端推流"；本项目的正确选择。
- **Serverless 心智**：函数无状态、冷启动、全局变量仅在热实例内共享——直接影响"向量库何时重建"。
- **Prompt 工程**：system prompt 写死规则（拒答边界、不露身份）；上下文 = 历史 + 检索片段 + 问题。
- **前端安全**：LLM 输出含任意 Markdown，必须用 `DOMPurify` 净化后再 `innerHTML`。
- **成本控制**：检索分数不够就**不调 LLM**，把"拒答"做成零成本动作。
- **密钥管理**：`.env` gitignore + 平台环境变量，Key 永不进仓库。
- **Serverless 状态陷阱**：函数无状态、热实例复用、跨函数内存不共享——"全局变量"在 Serverless 里既会串台又会丢状态，必须用 session / 外部存储隔离。
- **RAG 与长上下文的关系**：RAG 并未被百万 token 窗口取代，二者是协作关系（详见 `RAG_vs_长上下文深度分析.md`）——成本差 20–30 倍、长上下文有"中间遗忘"、实时更新场景 RAG 增量秒级生效。

### 3.2 实践经验

- **部署踩坑要敢换平台**：机制性限制（如 FC 的 header 注入）靠代码绕不过。
- **依赖要瘦**：一个 LangChain 让包体翻 8 倍，自写 100 行反而更可控。
- **产品化思维**：意图门槛、安全话术、状态/内容分离——这些"非功能"恰恰是面试加分项。
- **文档必须跟代码走**：README 一旦过时就是负资产（见 1.3）。

---

## 4. 学习方式的局限性 + 如何变主动

### 4.1 被动"跟着 AI 复制粘贴"的代价

- **幻觉式胜任**：东西跑起来了，但你说不清"为什么这样写、换个输入会怎样"。面试一追问就露馅。
- **不可迁移**：记住的是"这次的复制步骤"，不是"背后的原理"，下一个项目又得从零被带。
- **不会调试**：代码不是你写的，报错时只能把红字贴回给 AI，没有自己的排查路径。
- **依赖风险**：AI 偶尔会错（本项目的 FC 死磕、README 没同步更新都是例子），没有人肉把关就会一直错下去。

### 4.2 公平地说——你已经做了这些主动判断

别把自己说成纯工具人，这几件事是**你**的功劳，面试要敢认：
- **你发现了 UX bug**（初始化提示混入答案）——这是真实的产品直觉，不是 AI 提的。
- **安全话术 / 意图门槛**的产品化设计，是在你的方向下做的取舍。
- **演示视频脚本 + 面试自测清单**，说明你在为"展示"做主动规划。

### 4.3 转为主动学习者的 6 个具体做法

1. **先预测，再粘贴**：AI 给代码前，先自己猜"这段大概干嘛"，贴完对照猜错在哪。
2. **费曼复述**：每个 AI 片段，逼自己用一句中文说清"它为什么这样写"。说不出 = 没懂。
3. **故意改坏**：把阈值 0.12 改成 0.9、把融合权重 0.6/0.4 反过来，看行为怎么变——这是建立因果感最快的方法。
4. **关掉 AI 重建一次**：挑最小的一块（比如 `cosineSimilarity` 或 `chunkText`），不看 AI 自己重写，能写出来才是真会。
5. **读一手文档**：BM25、SSE、Vercel Functions 直接看官方文档，把 AI 当"审稿人/橡皮鸭"而不是"作者"。
6. **建学习日志**：每天记"今天我真正搞懂了 X"，而不是"今天 AI 帮我跑通了 X"。

---

## 5. 求职价值评估

### 5.1 这段经历值不值钱

**值钱的点**：这是一个**端到端、已部署、带工程优化**的 AI 应用，不是玩具 demo。混合检索 + 重排序 + 成本控制 + 安全设计，已经摸到"生产级 RAG"的门槛，高于绝大多数培训班作品。对**广深方向的 AI 应用 / 前端友好岗**很有说服力。

**不值钱的点**：无自动化测试、无向量持久化、Java 空壳——会让面试官怀疑深度；但"代码没进 git、文档过期"两条已通过本次提交 + README 重写解决。

### 5.2 面试怎么展示（讲决策，不讲步骤）

- 用 90 秒讲清：**为什么用混合检索**（召回更稳）、**为什么加重排序**（提准）、**为什么设意图门槛**（省钱防滥）、**SSE 为什么比轮询好**。
- 能徒手画 RAG 数据流图，能解释"BM25 和向量检索区别"。
- 准备一句兜底："Demo 是单机内存向量库，生产我会接 pgvector + 加鉴权限流"——展现你知道边界。
- 带**演示视频 + 公网链接**，比 PPT 强十倍。

### 5.3 怎么弥补"依赖 AI"的短板

- **诚实但有策略**：可以说"我用 AI 辅助快速验证架构，但核心模块我自己重写并理解了"——然后用 4.3 的"关 AI 重建"证明。
- **挑一个模块做'纯净版'**：比如把 `retrieve()` 用纯 Node 从零写一遍、补单元测试，放进仓库当"这是我独立写的"证据。
- **确保能逐行解释 `api/rag.js`**：这是你作品集的脸面，每行都得答得上"为什么"。

### 5.4 还需补充的技能 / 深度（按优先级）

1. **立即**：`git commit` 当前 Vercel 版，写干净的提交信息；同步更新 README（否则作品集自相矛盾）。
2. **高**：接真实向量库（Chroma/pgvector）做持久化；补 1-2 个检索/拒答单测。
3. **中**：支持 PDF/Word  ingestion；加鉴权限流（既然公网）。
4. **中**：补一份 RAG 评估（如 RAGAS 思路）和可观测，体现"不只跑通，还能量化好坏"。
5. **差异化**：再做一个**不同技术栈**的项目（比如把核心用 Python + LangChain 重写，或真把那个 Spring Boot 空壳做成可运行的 Java 版），证明广度——AI 岗面试常默认 Python，能用对方语言讲清楚会加分。
6. **底层**：能聊 scaling / 延迟 / 成本权衡（系统设计的味道），而不只是"功能实现了"。

### 5.5 达到"可基本投递实习"水平的准备清单

针对"实习生"定位（不要求深度，但要求**能讲清楚、能上手**），按优先级：

- **简历侧**：把本项目写成 1 段 STAR（情境/任务/行动/结果），尽量量化——如"用意图门槛把无关问题拒答做成零 token 消耗""会话隔离修复堵住多设备串台"。写"端到端 AI 应用"，别写"我懂深度学习"。
- **代码侧**：能逐行讲解 `api/rag.js`；挑一个核心模块（如 `retrieve()` 混合检索）**脱离 AI 用纯 Node 重写一遍并补单测**，作为"这是我独立实现的"硬证据。
- **原理侧**：能手画 RAG 数据流图、解释 BM25 与向量检索区别、说清"为什么 RAG 没被长上下文取代"（结合 `RAG_vs_长上下文深度分析.md`）。
- **广度侧**：补一个 **Python 版**（AI 岗默认 Python，能用对方语言讲会加分）或把那个 Java 空壳做成真·最小可运行版，证明技术栈不窄。
- **基础侧**：刷数据结构 / 算法（实习笔试）、HTTP/REST、数据库基础；Git 已会用 `commit`，再练 `branch` / `PR` / `rebase` 即可应对协作。
- **心态侧**：投递方向选"前端 / AI 应用 / 实习不限"，把本项目当"我能独立做出来一个能跑、能部署、能讲设计的 AI 应用"的证据——这已经超过大部分同届简历。

---

## 附录：建议你现在就做的 3 件事（状态更新）

1. ✅ `git commit` Vercel 版 —— 已完成（含会话隔离修复 + 两份分析文档）。
2. ✅ 重写 README 三节对齐 Vercel 架构 —— 已完成。
3. ⏳ 决定 Java 空壳（`pom.xml` + `RagDemoApplication.java`，仅一行 `spring.application.name`）去留：要么删掉，要么真写一个最小可运行版（否则面试被问会尴尬）。
